{% extends '::base.html.twig' %}

{% block body %}
    <h1>Tariff creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('tariff') }}">
            {% trans %}Back{% endtrans %}
        </a>
    </li>
</ul>
{% endblock %}
